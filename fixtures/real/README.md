# Real sliced g-code fixtures

Drop any real sliced `*.gcode` files here; the plr-gcode fixture tests
auto-discover them and run the full parse + simulate + Z-scan pipeline
over each one (the suite passes vacuously while this directory is empty).
